<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="org.jraf.android.dcn"
    xmlns:android="http://schemas.android.com/apk/res/android"
    >

    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Needed for Crashlytics -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.App">

        <!-- Google Play Services -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <!-- Crashlytics -->
        <meta-data
            android:name="com.crashlytics.ApiKey"
            android:value="439fd08746c1c52a7eb5e9fe0d2e0b631861d890" />

        <!-- AddressInfo list -->
        <activity
            android:name="org.jraf.android.dcn.handheld.app.addressinfo.list.AddressInfoListActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- AddressInfo edit -->
        <activity
            android:name="org.jraf.android.dcn.handheld.app.addressinfo.edit.AddressInfoEditActivity"
            android:theme="@style/Theme.App.Edit" />

        <!-- Geofencing service -->
        <service android:name="org.jraf.android.dcn.handheld.app.geofencing.GeofencingService" />

        <!-- Boot receiver -->
        <receiver android:name="org.jraf.android.dcn.handheld.app.boot.BootCompletedBroadcastReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
